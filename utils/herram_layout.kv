
<HerramLayout>:
    size_hint: (1,1)
    orientation: 'horizontal' 
    ToggleDedo:
        text:'DIM'
        group: 'herram' 
        controller:root.controller
        state: 'down'
    Label:
    ToggleDedo:
        text:'FUE'
        group: 'herram' 
        controller:root.controller
    Label:
    ToggleDedo:
        text:'VAC'
        group: 'herram' 
        controller:root.controller
    Label:
    ToggleDedo:
        text:'TEM'
        group: 'herram' 
        controller:root.controller
    Label:
    ToggleDedo:
        text:'ELE'
        group: 'herram' 
        controller:root.controller
    Label:
    ToggleDedo:
        text:'OTR'
        group: 'herram' 
        controller:root.controller
    Label:
